{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "File dialogs only; the save logic never touches the network.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
